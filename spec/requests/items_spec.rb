require 'rails_helper'

RSpec.describe "Items", type: :request do
  # describe "GET api/v1/bucketlists/:id/items" do
  #   it "works! (now write some real specs)" do
  #     get items_index_path
  #     expect(response).to have_http_status(200)
  #   end
  # end
end

# it "is authenticated with a token" do
#     put "/api/v1/users/#{@user.id}?user_email=#{@user.email}&auth_token=#{@user.authentication_token}", 
#     {user: {name: "New Name"}}, { "HTTP_AUTHORIZATION"=>"Token token=\"#{@api_key.access_token}\"" }
#     response.status.should be(201)
#   end